name: flutter_berita_api
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  http: ^1.1.0
  intl: ^0.18.1
  url_launcher: ^6.2.1
  webview_flutter: ^4.4.2
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  fluttertoast: ^8.2.4
  flutter_launcher_icons: ^0.13.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/brand.png"

flutter:
  uses-material-design: true
  assets:
    - assets/images/
